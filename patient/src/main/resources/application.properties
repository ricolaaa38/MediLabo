spring.application.name=patient
server.port=8080
#spring.data.mongodb.host=${SPRING_DATA_MONGODB_HOST}
#spring.data.mongodb.port=${SPRING_DATA_MONGODB_PORT}
#spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE}

internal.secret=${INTERNAL_SECRET}

spring.datasource.url=jdbc:postgresql://${SPRING_DATA_POSTGRES_HOST:postgres}:${SPRING_DATA_POSTGRES_PORT:5432}/${SPRING_DATA_POSTGRES_DATABASE:patient}
spring.datasource.username=${SPRING_DATA_POSTGRES_USERNAME:postgres}
spring.datasource.password=${SPRING_DATA_POSTGRES_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update
